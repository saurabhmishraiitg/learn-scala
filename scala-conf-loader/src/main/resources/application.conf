org.nexus {
      default {
        whoami = "my-app"
        master = "local"
        log-level = "INFO"
      }
      mysql {
        url = "jdbc:mysql://localhost/ram"
        username = "root"
        password = "mysql"
      }
}
